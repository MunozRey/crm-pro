> [!NOTE]
> Historical snapshot: this phase file is kept for audit/history and may be outdated.
> Source of truth for current status and priorities: `.planning/STATE.md` and `.planning/ROADMAP.md`.

> [!NOTE]
> Historical snapshot: this phase document is preserved for implementation history.
> Source of truth for current status and priorities:  and .

> **Historical Snapshot:** This phase document is retained for historical context. Current source of truth is `.planning/STATE.md` and `.planning/ROADMAP.md`.

---
plan: "01.3"
status: complete
completed: 2026-03-31
---

# Plan 01.3 — Summary

## One-liner
Created the gmail_tokens table with user-and-org-scoped RLS to enforce server-side-only storage of Gmail OAuth refresh tokens, preventing them from ever reaching the browser.

## What was built

- `supabase/migrations/20260331000003_create_gmail_tokens.sql` — DDL for the `gmail_tokens` table including:
  - 10 columns: `id`, `user_id`, `organization_id`, `access_token`, `refresh_token`, `token_type`, `scope`, `expires_at`, `created_at`, `updated_at`
  - `refresh_token TEXT NOT NULL` — invalid token records rejected at the DB level
  - `scope TEXT NOT NULL` — always recorded at OAuth grant time
  - `access_token TEXT` (nullable) — short-lived, may be NULL between refreshes
  - `UNIQUE(user_id, organization_id)` — one token set per user per org
  - `ENABLE ROW LEVEL SECURITY`
  - RLS policy `users_own_gmail_tokens`: `user_id = auth.uid() AND organization_id = public.get_org_id()`
  - Composite index `idx_gmail_tokens_user_org` on `(user_id, organization_id)`
  - `set_updated_at` trigger reusing existing `public.handle_updated_at()` from schema.sql (no duplication)
  - Foreign keys: `user_id → auth.users(id) ON DELETE CASCADE`, `organization_id → public.organizations(id) ON DELETE CASCADE`

## Verification

All 6 automated checks passed:

| Check | Pattern | Result |
|-------|---------|--------|
| 1 | `CREATE TABLE IF NOT EXISTS public.gmail_tokens` | PASS |
| 2 | `refresh_token.*NOT NULL` | PASS |
| 3 | `UNIQUE(user_id, organization_id)` | PASS |
| 4 | `user_id = auth.uid()` | PASS |
| 5 | `ENABLE ROW LEVEL SECURITY` | PASS |
| 6 | `handle_updated_at` | PASS |

Manual SQL checks (to be run in Supabase SQL Editor after applying migration 001 then 003):
- Column nullability: `refresh_token` and `scope` must show `is_nullable = 'NO'`
- RLS enabled: `rowsecurity = true` in `pg_tables`
- Policy: `users_own_gmail_tokens` in `pg_policies`
- UNIQUE constraint: 1 row with `constraint_type = 'UNIQUE'`
- Index: `idx_gmail_tokens_user_org` in `pg_indexes`

## Deviations
None — plan executed exactly as written.

## Apply order
Migration files must be applied in filename order in the Supabase SQL Editor:
1. `20260331000001_create_org_tables.sql` (defines `organizations` table and `get_org_id()`)
2. `20260331000002_...` (if exists)
3. `20260331000003_create_gmail_tokens.sql` (this file — depends on organizations table and get_org_id())
