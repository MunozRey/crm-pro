---
phase: 05-core-data-realtime
plan: "05.3"
wave: 2
depends_on: ["05.1"]
files_modified:
  - src/store/dealsStore.ts
  - src/store/notificationsStore.ts
requirements: [DATA-03, DATA-05, DATA-06, DATA-07, DATA-08]
---

<objective>
Migrate dealsStore and notificationsStore from localStorage persist to async Supabase calls.
</objective>

<tasks>

<task>
  <name>Migrate dealsStore</name>
  <files>src/store/dealsStore.ts</files>
  <read_first>src/store/dealsStore.ts, src/types/index.ts, src/store/contactsStore.ts (for pattern)</read_first>
  <action>
Same pattern as contactsStore but dealsStore has cross-store side effects:
1. Remove `persist`, `LS_KEYS`, `onRehydrateStorage`
2. Add `isLoading`, `error`, `fetchDeals()`
3. Snake_case mappers for Deal type (expected_close_date, closed_at, etc.)
4. Modify CRUD to call Supabase — preserve the existing side effects that call useNotificationsStore, useAuditStore, useAutomationsStore after mutations
5. For `updateDealStage`: call Supabase update, then fire the same notification/audit/automation hooks
6. Keep all computed selectors (getByStage, getTotalPipeline, getMonthlyRevenue, etc.)
  </action>
</task>

<task>
  <name>Migrate notificationsStore</name>
  <files>src/store/notificationsStore.ts</files>
  <read_first>src/store/notificationsStore.ts, src/types/index.ts</read_first>
  <action>
1. Remove `persist`, `LS_KEYS`, custom localStorage storage, `onRehydrateStorage` seed
2. Add `isLoading`, `error`, `fetchNotifications()`
3. Snake_case mappers for CRMNotification type (entity_type, entity_id, user_id, triggered_by, is_read, created_at)
4. Modify add, markAsRead, markAllAsRead, deleteNotification to call Supabase
5. Keep `disabledTypes` as local-only state (not in DB — client preference)
6. Fallback: when !isSupabaseConfigured, use in-memory with seed notifications
  </action>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run` — tests still pass
</verification>
