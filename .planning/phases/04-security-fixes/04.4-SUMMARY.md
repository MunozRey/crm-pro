---
phase: "04"
plan: "04.4"
status: complete
completed: "2026-04-07"
---

# Phase 04.4 Summary — authStore Merge Guard & LocalStorage Hardening

## One-liner
Added Zustand persist merge guard to authStore so SEED_USERS/SEED_PASSWORDS are never overwritten by empty rehydrated state.

## What was done
- Added `merge` function to authStore persist config
- Guard ensures `users` and `passwords` always fall back to SEED values if rehydrated state is empty
- Fixed "Usuario no encontrado" login bug caused by stale localStorage wiping auth state

## Result
Login works reliably across sessions. localStorage rehydration bug permanently fixed.
