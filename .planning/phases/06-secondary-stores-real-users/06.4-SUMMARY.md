---
phase: "06"
plan: "06.4"
status: complete
completed: "2026-04-08"
---

# Phase 06.4 Summary — MOCK_USERS Replaced Everywhere

## One-liner
Replaced MOCK_USERS with real org users from authStore in all 9 files; "Assigned to" dropdowns now reflect real org members.

## What was done
- Removed `MOCK_USERS` import from: ContactForm, DealForm, ActivityForm, Contacts, Deals, Dashboard, Reports, PipelineTimeline, Forecast
- Replaced with `useAuthStore((s) => s.users)` selector mapped to `{ value: name, label: name }`
- Default `assignedTo` in forms now uses `orgUsers[0]?.name ?? ''`
- Leaderboard page removed from codebase (no longer needed — data was MOCK_USERS-driven)

## Result
No MOCK_USERS references remain in codebase. Dropdowns dynamically reflect real org members.
