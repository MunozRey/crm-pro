---
phase: 04-security-fixes
plan: "04.1"
wave: 1
files_modified:
  - src/store/aiStore.ts
  - src/pages/Settings.tsx
requirements: [SEC-02]
---

<objective>
Remove the Anthropic API key from localStorage/aiStore persist. The key must no longer be stored in the browser — it will live exclusively in the Edge Function env vars (wired in Phase 7).
</objective>

<tasks>

<task>
  <name>Remove apiKey from aiStore persist + Settings UI</name>
  <action>
1. In src/store/aiStore.ts: Remove `apiKey` and `setApiKey` from the store interface, state, and actions. Remove the `partialize` or persist config if it includes `apiKey`. Keep `openRouterKey` in persist (it's not Anthropic's key). If there's no `partialize`, add one that excludes `apiKey`.

2. In src/pages/Settings.tsx: Remove the Anthropic API Key input field, the `apiKeyInput` state, `showApiKey` state (if only used for Anthropic key), `handleSaveApiKey` handler, and the apiKey/setApiKey destructuring from useAIStore. Remove the UI section showing "Anthropic API Key" input and the `{apiKey && ...}` configured indicator.

Keep the OpenRouter key field and all model selection UI intact.

3. In src/services/aiService.ts: Remove any reading of `aiStore.apiKey`. The Anthropic calls will be stubbed in plan 04.3.
  </action>
</task>

</tasks>
