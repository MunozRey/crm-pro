> [!NOTE]
> Historical snapshot: this phase file is kept for audit/history and may be outdated.
> Source of truth for current status and priorities: `.planning/STATE.md` and `.planning/ROADMAP.md`.

> [!NOTE]
> Historical snapshot: this phase document is preserved for implementation history.
> Source of truth for current status and priorities:  and .

> **Historical Snapshot:** This phase document is retained for historical context. Current source of truth is `.planning/STATE.md` and `.planning/ROADMAP.md`.

---
phase: 02-supabase-auth
plan: "02.5"
status: complete
started: "2026-03-31T17:00:00.000Z"
completed: "2026-03-31T20:25:00.000Z"
commits:
  - hash: "8e35f56"
    message: "feat(02-02.5): fix logout() — add signOut, async, clear all state"
requirements: [AUTH-05, SEC-01]
---

# Plan 02.5 Summary — Logout Fix

## What was built

- `authStore.logout()` now calls `supabase.auth.signOut()` before clearing Zustand state
- `logout` type changed from `() => void` to `() => Promise<void>`
- Clears all four fields: `currentUser`, `session`, `supabaseSession`, `organization`
- `Topbar.tsx` and `Layout.tsx` callers updated to handle async logout
- Logout test filled: verifies `signOut` called and state cleared

## Key files

### Modified
- `src/store/authStore.ts` — async logout with signOut call
- `src/components/layout/Topbar.tsx` — void logout().then(() => navigate)
- `src/components/layout/Layout.tsx` — void logout().then() for session expiry
- `tests/auth/authStore.test.ts` — AUTH-05 logout test filled

## Self-Check: PASSED

- [x] logout calls supabase.auth.signOut() when configured
- [x] logout clears currentUser, session, supabaseSession, organization
- [x] logout typed as () => Promise<void>
- [x] All 19 tests pass
