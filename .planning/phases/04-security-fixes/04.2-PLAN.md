---
phase: 04-security-fixes
plan: "04.2"
wave: 1
files_modified:
  - src/pages/AIAgent.tsx
  - package.json
requirements: [SEC-03]
---

<objective>
Replace dangerouslySetInnerHTML + hand-rolled renderMarkdown() in AIAgent.tsx with react-markdown + rehype-sanitize. This closes the XSS vector.
</objective>

<tasks>

<task>
  <name>Install react-markdown and rehype-sanitize</name>
  <action>
Run: npm install react-markdown rehype-sanitize
  </action>
</task>

<task>
  <name>Fix AIAgent.tsx XSS</name>
  <action>
1. Remove the `renderMarkdown` function from AIAgent.tsx entirely.
2. Add imports: `import ReactMarkdown from 'react-markdown'` and `import rehypeSanitize from 'rehype-sanitize'`
3. Replace the two dangerouslySetInnerHTML usages:
   - For assistant messages (line ~315): Replace `dangerouslySetInnerHTML={msg.role === 'assistant' ? { __html: renderMarkdown(msg.content) } : undefined}` with a conditional: if assistant, render `<ReactMarkdown rehypePlugins={[rehypeSanitize]}>{msg.content}</ReactMarkdown>`; if user, render the text as a plain span/div.
   - For streaming text (line ~335): Replace `dangerouslySetInnerHTML={{ __html: renderMarkdown(streamingText) }}` with `<ReactMarkdown rehypePlugins={[rehypeSanitize]}>{streamingText}</ReactMarkdown>`

4. Audit the rest of the codebase for any other dangerouslySetInnerHTML usages and note them in the summary (do not blindly remove them — some may be intentional, just document findings).
  </action>
</task>

</tasks>
