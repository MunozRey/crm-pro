---
phase: 05-core-data-realtime
plan: "05.2"
wave: 2
depends_on: ["05.1"]
files_modified:
  - src/store/companiesStore.ts
  - src/store/activitiesStore.ts
requirements: [DATA-02, DATA-04, DATA-06, DATA-07]
---

<objective>
Migrate companiesStore and activitiesStore from localStorage persist to async Supabase calls. Follow the exact same pattern established in 05.1 (contactsStore).
</objective>

<tasks>

<task>
  <name>Migrate companiesStore</name>
  <files>src/store/companiesStore.ts</files>
  <read_first>src/store/companiesStore.ts, src/types/index.ts, src/store/contactsStore.ts (for pattern reference)</read_first>
  <action>
Same migration as contactsStore:
1. Remove `persist` middleware, `LS_KEYS`, `onRehydrateStorage` seed hook
2. Add `isLoading`, `error` state
3. Add `fetchCompanies()` — loads from Supabase or seedCompanies
4. Add snake_case ↔ camelCase mappers for Company type
5. Modify addCompany, updateCompany, deleteCompany to call Supabase when configured
6. Keep audit logging
7. Fallback to in-memory when !isSupabaseConfigured
  </action>
</task>

<task>
  <name>Migrate activitiesStore</name>
  <files>src/store/activitiesStore.ts</files>
  <read_first>src/store/activitiesStore.ts, src/types/index.ts</read_first>
  <action>
Same pattern:
1. Remove `persist`, `LS_KEYS`, `onRehydrateStorage`
2. Add `isLoading`, `error`, `fetchActivities()`
3. Add snake_case ↔ camelCase mappers for Activity type
4. Modify CRUD to call Supabase when configured
5. Keep all selectors (getOverdueActivities, getByContact, etc.) as-is — they read from local state
  </action>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run` — tests still pass
</verification>
